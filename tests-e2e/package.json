{
  "name": "@mini-hedge/tests-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright smoke tests for the three Next.js UIs (ui, ops-ui, client-ui).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:setup": "playwright test auth.setup.ts",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20"
  }
}
